The scripts in this directory and its subdirectories are on the $PATH and therefore are globally available.

The `commands` files specify additional aliases and functions that are globally available.

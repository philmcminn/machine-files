#!/usr/bin/env sh

git config --global user.email "phil@philmcminn.com"
git config --global user.name "Phil McMinn"
git config --global init.defaultBranch "main"
git config --global pull.rebase false

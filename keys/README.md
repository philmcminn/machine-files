This folder contains SSH/API keys for the machine, in an executable file, simply names `keys`.

These are set as environmental variables, and include:

```
export ANTHROPIC_API_KEY="key"

```
On a fresh machine, copy the above into a new `keys` file in this directory, adding in the appropriate keys.


